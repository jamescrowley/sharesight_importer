# Security policy

Report credential exposure or a destructive-import vulnerability privately through GitHub Security Advisories rather than a public issue. Do not attach credentials, access tokens, portfolio exports, or API captures.

No Sharesight secret is accepted on the command line. Verbose HTTP output redacts headers and bodies. Rotate credentials immediately if they may have been exposed. Back up a portfolio before any live import, especially destructive replacement.
