[project]
name = "sharesight-importer"
version = "0.1.0"
description = "Import bespoke transaction and price CSVs into Sharesight"
readme = "README.md"
requires-python = ">=3.12,<3.15"
dependencies = [
    "curlify>=2.2.1,<3",
    "requests>=2.32.0,<3",
]

[tool.uv]
package = false
